# secrets — NEVER commit env files
.env
backend/.env
*.env

# python
__pycache__/
*.pyc
backend/.venv/
backend/venv/
.venv/
venv/

# vector stores / local data artifacts
backend/qdrant_data/
data/qdrant_local/
*.pkl
# … except the BM25 index: app/retrieval.py refuses to boot without it,
# and rebuilding it (build_index.py) re-embeds the whole corpus.
!data/bm25.pkl

# databases
backend/*.db
*.db

# node
node_modules/
frontend/dist/

# playwright artifacts
frontend/test-results/
frontend/playwright-report/

# local git worktrees
.worktrees/

# local agent scratch reports
.superpowers/

# TTS voice audition samples (local only)
voice-samples/

# private secrets bundle (never commit)
team-secrets/
